# Build output (shipped as a release asset, never committed)
FS25_SettingsHub.zip

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp

# Pre-commit gate deps (tools/test/ - installed by npm, never shipped)
node_modules/
